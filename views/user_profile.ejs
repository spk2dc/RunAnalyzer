<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.firstname %>'s Profile</title>
</head>

<body>
    <h1><%= user.firstname %> <%= user.lastname %></h1>
    <img src="<%= user.profile %>" alt="user picture">
    <h3>Username: <%= user.username %></h3>
    <h3>ID: <%= user.id %></h3>
    <h3>Location: <%= user.city %>, <%= user.state %></h3>
    <h3>Account Created: <%= new Date(user.created_at) %></h3>

    <table>
        <thead>
            <th>Name</th>
            <th>Distance</th>
            <th>Moving Time</th>
            <th>Elapsed Time</th>
            <th>Activity Type</th>
            <th>Date</th>
            <th>Time</th>
            <th>Average Speed</th>
            <th>Max Speed</th>
        </thead>
        <tbody>
            <% for(let i=0; i < activities.length; i++) { %>
                <% let fullDate = new Date(activities[i].start_date_local) %>
            <tr>
                <td><%= activities[i].name %></td>
                <td><%= activities[i].distance %></td>
                <td><%= activities[i].moving_time %></td>
                <td><%= activities[i].elapsed_time %></td>
                <td><%= activities[i].type %></td>
                <td><%= fullDate.toLocaleDateString('en-US', { year: 'numeric', month: '2-digit', day: '2-digit' }) %></td>
                <td><%= fullDate.toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit', second: '2-digit' }) %></td>
                <td><%= activities[i].average_speed %></td>
                <td><%= activities[i].max_speed %></td>
            </tr>
            <% } %>
        </tbody>
    </table>
</body>

</html>